[project]
name = "shiftcare-mcp-evals"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = ["deepeval==4.2.0"]

[dependency-groups]
dev = ["pytest"]

[tool.pytest.ini_options]
python_files = ["test_*.py"]
